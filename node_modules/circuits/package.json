{
    "name": "circuits",
    "version": "1.0.0",
    "description": "ZK Circuits for BracU Email Verification",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "compile": "mkdir -p build && circom src/bracu_verifier.circom --r1cs --wasm --sym --c -o build",
        "gen-regex": "zk-regex \"from:[^\\r\\n]*@g\\.bracu\\.ac\\.bd\" -t src/bracu_regex.circom -n BracuRegex"
    },
    "dependencies": {
        "@zk-email/circuits": "^6.0.0",
        "@zk-email/helpers": "^6.0.0",
        "@zk-email/zk-regex-circom": "^1.2.0",
        "snarkjs": "^0.7.0"
    },
    "devDependencies": {
        "circomlib": "^2.0.5"
    }
}