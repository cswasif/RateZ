{
    "name": "@zk-ratemyprof/api",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "test": "vitest",
        "db:migrate": "wrangler d1 execute zk-ratemyprof --local --file=./src/db/schema.sql",
        "db:seed": "wrangler d1 execute zk-ratemyprof --local --file=./src/db/seed.sql"
    },
    "dependencies": {
        "hono": "^4.0.0",
        "@aztec/bb.js": "0.84.0",
        "@noir-lang/noir_js": "1.0.0-beta.5",
        "@noir-lang/noirc_abi": "1.0.0-beta.5"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240117.0",
        "typescript": "^5.0.0",
        "vitest": "^1.0.0",
        "wrangler": "^3.0.0"
    }
}